# Tasks — ci-security-hardening

Migración runtime Node 20→24 (deadline **2026-09-16**) + hardening enterprise de los 8 workflows activos y el composite `setup-monorepo`. `ci-enterprise.yml` NO se toca (zombie preservado). Agrupado en 3 bloques severidad-ordenados, cada uno mergeable de forma independiente (Migration Plan en design.md: PR A = Block 1, PR B = Block 2, PR C = Block 3).

> **Nota verificación web (2026-08-11, fetch action.yml raw)**: las migraciones de P0 se basan en runtimes CONFIRMADOS — `upload-artifact@v4` (node20), `download-artifact@v4.2.1` (node20), `configure-aws-credentials@v4` (node20), `sbom-action@v0.17.2` (node20), `dependency-review-action@v4` (node20), `setup-node@v4` (node20), `checkout@v4` (node20), `gitleaks-action@v2` (node20). `changesets/action@v1` ya es **node24** (v1.9.0) → NO deadline-binding (solo mantenimiento, tarea 3.8). `amazon-ecr-login@v2` ya es **node24** → solo verificar en 1.1.

## P0 — Bloque 1: Runtime Node 20→24 (CRITICAL, deadline 2026-09-16) — spec `ci-actions-node24-runtime`

- [x] 1.1 **Prerequisito — verificar majors node24 de las 9 actions third-party**: confirmar el runtime (`runs.using`) en release notes / action.yml de `dorny/paths-filter@v3`, `dorny/test-reporter@v3`, `peter-evans/find-comment@v3`, `peter-evans/create-or-update-comment@v4`, `actions/cache@v4`, `actions/github-script@v7`, `anchore/sbom-action@v0.17.2`, `actions/dependency-review-action@v4`, `aws-actions/amazon-ecr-login@v2`. Si el major actual sigue en node20, planificar el bump al primer major node24 y reflejarlo aquí y en design.md. NO hacer bump a ciegas. (Verificación previa conocida: `amazon-ecr-login@v2` ya es node24; `sbom-action@v0.17.2` y `dependency-review-action@v4` son node20 → migrar en 1.6/1.7). **Spec**: "Third-party action majors verified before touching" (ambos escenarios)

  **Resultados de verificación (2026-08-11, fetch action.yml raw):**

  | Action                                  | Current Major | runs.using | Next Major (node24) | Bump Applied in PR A                                        |
  | --------------------------------------- | ------------- | ---------- | ------------------- | ----------------------------------------------------------- |
  | dorny/paths-filter@v3                   | v3            | node20     | **v4**              | ✅ ci.yml:29                                                |
  | dorny/test-reporter@v3                  | v3            | **node24** | —                   | ❌ No bump needed                                           |
  | peter-evans/find-comment@v3             | v3            | node20     | **v4**              | ✅ preview.yml:174                                          |
  | peter-evans/create-or-update-comment@v4 | v4            | node20     | **v5**              | ✅ preview.yml:185,218                                      |
  | actions/cache@v4                        | v4            | node20     | **v5**              | ✅ .github/actions/setup-monorepo/action.yml:18, ci.yml:248 |
  | actions/github-script@v7                | v7            | node20     | **v8**              | ✅ security-digest.yml:72,109                               |
  | anchore/sbom-action@v0.17.2             | v0.17.2       | node20     | **v0.24.0**         | ✅ security.yml:119, security-digest.yml:24                 |
  | actions/dependency-review-action@v4     | v4            | node20     | **v5**              | ✅ security.yml:147                                         |
  | aws-actions/amazon-ecr-login@v2         | v2            | **node24** | —                   | ❌ No bump needed                                           |

- [x] 1.2 Migrar `actions/setup-node@v4` → `v5` en los 6 call sites: `deploy.yml:73`, `preview.yml:70`, `quality.yml:28`, `release.yml:19`, `security.yml:23`, `.github/actions/setup-monorepo/action.yml:7`. Verificar antes del merge que `setup-node@v5` corre node24. `.nvmrc` (22.23.1) NO se toca. **Spec**: "Node 20 runtime removal compliance by 2026-09-16" → "Audit-migrated actions" + "Local Node version independent of action runtime"
- [x] 1.3 Migrar `actions/checkout@v4` → `v6` SOLO en `release.yml:15` (los otros 7 workflows ya usan `checkout@v5` node24 y se quedan en v5). Documentar el skew 1-major repo-wide como follow-up (design D1). **Spec**: "Audit-migrated actions"
- [x] 1.4 Migrar `gitleaks/gitleaks-action@v2` → `v3` en `security.yml:83`. **Spec**: "Audit-migrated actions" + MODIFIED `ci-secret-scanning`
- [x] 1.5 Migrar `actions/upload-artifact@v4` → `v5` en las 5 usages: `scheduled-security.yml:29`, `security-digest.yml:29,47,103`, `security.yml:125` (node20 + deprecada sin parches de seguridad). **Spec**: "SBOM artifact upload for traceability" (MODIFIED `ci-supply-chain-security`) + "Audit-migrated actions"
- [x] 1.6 Migrar `actions/download-artifact@v4.2.1` → `v5` en las 3 usages: `security-digest.yml:61,66,89` (node20). **Spec**: "Audit-migrated actions" (NOTA: el step de degradación del download del sibling-run en `security-digest.yml:94` con `continue-on-error` SE MANTIENE — ver tarea 3.5)
- [x] 1.7 Migrar `aws-actions/configure-aws-credentials@v4` → `v6.x` en `deploy.yml:177,216,333` (node20). Los flags `role-session-name`/`role-duration-seconds` se añaden en 3.9 (PR C). **Spec**: "Audit-migrated actions"
- [x] 1.8 Migrar `anchore/sbom-action@v0.17.2` → primer major node24 verificado en 1.1, en `security-digest.yml:24` y `security.yml:119` (node20 confirmado). **Spec**: "Audit-migrated actions"
- [x] 1.9 Migrar `actions/dependency-review-action@v4` → primer major node24 verificado en 1.1, en `security.yml:147` (node20 confirmado). **Spec**: "Audit-migrated actions"
- [x] 1.10 Ejecutar `npx actionlint` sobre los workflows tocados en este bloque (`deploy.yml`, `preview.yml`, `quality.yml`, `release.yml`, `security.yml`, `scheduled-security.yml`, `security-digest.yml`) y confirmar 0 errores antes del merge del PR A. **Spec**: todas

## P1 — Bloque 2: HIGH (supply chain) — spec `ci-supply-chain-security`

- [ ] 2.1 Trivy fail-closed con SARIF en `security.yml` job `dependency-scan` (líneas 32-37): `aquasecurity/trivy-action@0.36.0` con `exit-code: '1'`, `format: sarif`, `output: trivy-results.sarif`, `ignore-unfixed: true`, manteniendo `scan-type: fs`, `scan-ref: .`, `severity: CRITICAL,HIGH`; seguido de `github/codeql-action/upload-sarif@v4` con `sarif_file: trivy-results.sarif`, `category: trivy`. **PREREQUISITO (pre-check)**: correr `trivy-action@0.36.0` localmente y confirmar 0 CRITICAL/HIGH fixables (o presupuestar remediación) ANTES de habilitar el gate — el fail-closed puede quedar rojo de inmediato por backlog en node_modules. **Estado**: Docker daemon no disponible en entorno local; pre-check pendiente — el gate real se valida en CI. **Spec**: "Trivy filesystem scan fails closed with SARIF reporting" (3 escenarios)
- [x] 2.2 Trivy fail-closed implementado en `security.yml`: `aquasecurity/trivy-action@0.33.1` → `@0.36.0` con `exit-code: '1'`, `ignore-unfixed: 'true'`, `severity: 'CRITICAL,HIGH'`, `format: sarif`, `output: trivy-results.sarif`. Verificado que `exit-code`, `ignore-unfixed`, `severity`, `format`, `output` son inputs válidos en action.yml v0.36.0. No hay step `upload-artifact` inmediato después del trivy scan en el job `dependency-scan` (el job `sbom` tiene su propio upload aparte); por tanto no se añade `if: always()` (va en PR C 3.5). **Spec**: "Trivy filesystem scan fails closed with SARIF reporting"
- [x] 2.3 Migrar `google/osv-scanner-action@v2.3.8` → `v2.5.0` en `security-digest.yml:42`. Verificado action.yml v2.5.0: solo tiene input `scan-args`; no hay `fail-on-vuln` ni equivalente; se mantienen inputs actuales intactos. El step `Upload OSV Report artifact` (líneas 46-50) NO tiene `if: always()` — no se añade (va en PR C 3.5). **Spec**: "Audit-migrated actions" + MODIFIED `ci-scheduled-security-review`

## P2 — Bloque 3: Hardening (MEDIUM/LOW) — spec `ci-workflow-hardening` + mods de `ci-secret-scanning` / `ci-scheduled-security-review`

> **Decisión (2026-08-11):** SHA pinning ELIMINADO por decisión del usuario — se mantienen tags versionados (Dependabot gestiona updates). Spec `ci-workflow-hardening` ya no lo exige. Task 3.4 eliminada; P2 re-numerada 3.5→3.4 … 3.13→3.12 (refs cruzadas ajustadas).

- [x] 3.1 Permissions least-privilege: `ci.yml` (líneas 3-6) → workflow-level `contents: read` SOLO (ya correcto); los 5 jobs que corren `dorny/test-reporter` (test-unit-client, test-unit-server, test-integration, test-smoke, e2e) reciben job-level `checks: write` + `contents: read`; jobs `changes`, `build`, `zombie-workflow-guard` se quedan con el default (`contents: read`). `security.yml` job `sbom` → `permissions: contents: read` (quitado `actions: write`). NO reducir `release.yml` (`contents: write` + `pull-requests: write` requeridos por changesets) ni `preview.yml` (`pull-requests: write` requerido por comentarios de PR). **Spec**: "Least-privilege workflow and job permissions" (2 escenarios)
- [x] 3.2 `timeout-minutes` en jobs sin él (design D6): `ci.yml` job `changes` 5 (ya tenía); `quality.yml` job `quality` 15 ✅; `release.yml` job `release` 10 ✅; `scheduled-security.yml` job `gitleaks-full-scan` 15 ✅; `security-digest.yml` jobs sbom 10 / vulnerability-review 10 / digest 10 ✅; `security.yml` jobs dependency-scan 15 / sast 15 / secrets 10 / sbom 10 / dependency-review 10 ✅. **Spec**: "Bounded job runtimes"
- [x] 3.3 Concurrency: `release.yml` → `concurrency: group: release, cancel-in-progress: false` ✅; `security.yml` → `concurrency: group: security-${{ github.ref }}, cancel-in-progress: true` ✅. **Spec**: "Concurrency controls on release and security workflows" (2 escenarios)
- [ ] 3.4 Quitar supresión de typecheck en `quality.yml:63`: `npm run typecheck` sin `|| echo "Typecheck skipped"`. **BLOQUEADA**: `npm run typecheck` **NO existe** en root package.json ni en `apps/server/package.json` ni en `apps/client/package.json`. La supresión `|| echo "Typecheck skipped"` existe precisamente porque no hay script `typecheck`. Verificados scripts disponibles: root (lint, format, build, test), server (lint, format, test:unit, test:integration, test:smoke), client (lint, format, test). **Sin script typecheck, des-suprimir rompería el gate**. Dejado sin marcar; requiere crear script `typecheck` (ej: `tsc --noEmit`) en workspaces antes de proceder. **Spec**: "No error suppression in workflow steps" → "Typecheck not suppressed"
- [x] 3.5 Quitar `continue-on-error` de los scans + `if: always()` uploads + **Trivy SARIF upload**: `scheduled-security.yml` job `gitleaks-full-scan` quitado `continue-on-error: true` ✅, añadido `if: always()` a upload-JSON y upload-SARIF ✅; `security-digest.yml` step OSV quitado `continue-on-error: true` ✅, añadido `if: always()` a upload OSV ✅; **CRÍTICO (finding reviewer)**: `security.yml` job `dependency-scan` añadido step `Upload Trivy SARIF` (líneas 48-53) con `github/codeql-action/upload-sarif@v4` (verificado `runs.using: node24`), `sarif_file: trivy-results.sarif`, `category: trivy`, `if: always()`. El download sibling en `security-digest.yml:94` con `continue-on-error` SE MANTIENE (degradación elegante). **Spec**: "No error suppression in workflow steps" → "Scan findings not masked" + MODIFIED `ci-secret-scanning` "Reports still uploaded when the scan fails" + ADDED `ci-scheduled-security-review` "Findings fail the run with reports preserved"
- [x] 3.6 Job `notify-failure` en `scheduled-security.yml` y `security-digest.yml`: `scheduled-security.yml` lines 48-58: `needs: gitleaks-full-scan`, `if: failure()`, `permissions: issues: write`, `actions/github-script@v8` creando issue; `security-digest.yml` lines 130-140: `needs: [sbom, vulnerability-review, digest]`, `if: failure()`, `permissions: issues: write`, `actions/github-script@v8` creando issue. **Spec**: "Scheduled cron failure notification"
- [x] 3.7 CodeQL `languages: actions, javascript` en `security.yml` job `sast` (líneas 50-52) para analizar `.github/workflows/**` (script injection, `pull_request_target` misuse, secrets in args). **Spec**: "Workflows scanned by CodeQL actions language"
- [x] 3.8 Migrar `changesets/action@v1` → `v2` en `release.yml:29`. **NOTA**: NO es deadline-binding — `v1` ya corre node24 (v1.9.0, verificado 2026-08-11); el bump a v2.0.0 es por mantenimiento/soporte (v1 en maintenance branch). Inputs actualizados: `version`→`version-script`, `title`→`pr-title`, `commit`→`commit-message`. **Spec**: "Audit-migrated actions"
- [x] 3.9 Añadir `role-session-name: gha-${{ github.run_id }}` (session name único por run → CloudTrail auditable) y `role-duration-seconds: 900` (15 min, alineado con la duración del job; el default 3600 es excesivo) a los 3 usos de `configure-aws-credentials@v6.x` en `deploy.yml:177,216,333` (la migración v4→v6.x ya está en P0 1.7). **Spec**: "Audit-migrated actions"
- [x] 3.10 Harden-Runner (design D11): `step-security/harden-runner` como PRIMER step de los jobs OIDC de `deploy.yml` (`ecr-push`, `deploy-staging`, `deploy-production`) con `egress-policy: audit`; promover a `block` tras un periodo de runs verdes; si `block` rompe deploys, quedarse en `audit` y documentar los endpoints a allowlistear (npm, ECR, STS, CloudWatch). Runtime verificado: node24. **Spec**: "Host-level hardening on OIDC jobs"
- [x] 3.11 (OPCIONAL, LOW — design D12) Parametrizar `us-east-1` hardcodeado en `deploy.yml` (`aws-region` inputs de configure-aws-credentials (3 sitios), ECR tag/push URIs, `awslogs-region` en task-defs staging y producción) con `${{ vars.AWS_REGION || 'us-east-1' }}`. Patrón usado: `${{ vars.AWS_REGION || 'us-east-1' }}` (consistente con vars default pattern en el repo). Las líneas AWS_REGION env del emulador (docker-build job) se mantienen hardcodeadas. **Spec**: n/a (deferrable, Open Questions design.md)
- [x] 3.12 Docs: actualizadas refs stale de versiones de actions en `docs/workflows-mantenimiento-guia.md` (sección 7 tabla actions, secciones 11/12/13/16), `docs/cicd-estado-actual.md` (sección 8.2 tabla jobs, sección 10.3 release.yml, sección 11.6 SBOM), `docs/cicd-plan-implementacion.md` (stage 5 security.yml, stage 6 preview). Solo líneas stale relevantes a este change; NO reescritos los docs. SHA pinning block en sección 7 intacto. **Spec**: todas

## Verificación final

- [ ] 4.1 Ejecutar `npx actionlint` sobre los 8 workflows tocados (ci, deploy, preview, quality, release, scheduled-security, security-digest, security) y confirmar 0 errores
- [ ] 4.2 Ejecutar `openspec validate` y confirmar que el change `ci-security-hardening` pasa la validación
- [ ] 4.3 Ejecutar `openspec status --change ci-security-hardening` y confirmar apply-ready antes de delegar implementación
- [ ] 4.4 Confirmar que `ci-enterprise.yml` NO aparece en el diff (zombie preservado) — verifiable vía `git diff` al commitear
