## 1. Prompt Guard Unification (D1 Hybrid — all 8 prompts)

- [x] 1.1 Add a new top-of-file `## CRITICAL RULES` section to `docs/opencode/prompts/spec-manager.md` between line 3 (`# SPEC-MANAGER SYSTEM PROMPT`) and `## YOUR IDENTITY` (NOT mid-file at ~line 78), replicating `developer.md:3-11`: the note line (`> These rules are repeated at the bottom (OUTPUT CONTRACT section). If you update one, update both.`) + 3 bullets — (1) envelope XML obligatorio: `- **Your response MUST be wrapped in \`<output-contract agent="spec-manager" version="1">{...}</output-contract>\` XML envelope.**`, (2) `- **Empty responses are NOT acceptable.**`, (3) `- **Do NOT end without emitting the structured deliverable.**` — followed by a `---` separator
- [x] 1.2 Add a new top-of-file `## CRITICAL RULES` sandwich to `docs/opencode/prompts/git-manager.md` (after the `# GIT-MANAGER SYSTEM PROMPT` h1, before `## YOUR IDENTITY`): note line + 3 bullets matching the `developer.md:5-11` pattern (bullets, NOT numbered items); rename the legacy `# CRITICAL RULES` block at line 71 to `## Behavioral Rules` (items 1-9 intact); do NOT add any numbered guard item to the renamed block (avoid third variant)
- [x] 1.3 Add a new top-of-file `## CRITICAL RULES` sandwich to `docs/opencode/prompts/project-manager.md`: note line + 3 bullets matching the `developer.md:5-11` pattern; rename the legacy `# CRITICAL RULES` block at line 146 to `## Behavioral Rules` (items 1-8 intact); do NOT add any numbered guard item
- [x] 1.4 Add a new top-of-file `## CRITICAL RULES` sandwich to `docs/opencode/prompts/orchestrator.md` (after line 3 `# ORCHESTRATOR SYSTEM PROMPT`, before `## YOUR IDENTITY`): note line + 3 bullets matching the `developer.md:5-11` pattern (envelope bullet with `agent="orchestrator"`, empty-response, structured-deliverable); rename the legacy `# CRITICAL RULES` block at line 354 (26 items) to `## Behavioral Rules` (items intact); rationale: the orchestrator's own final responses are NOT hook-validated (plugin fires only on the `task` tool), so this sandwich is its only anti-empty guard

## 2. Layer-3 Retry Protocol (orchestrator.md)

- [x] 2.1 Add "## LAYER-3 RETRY PROTOCOL (SILENT EXIT)" section to `docs/opencode/prompts/orchestrator.md` defining: parse `<task_result>` after every `task` call; empty body or missing `<output-contract>` envelope → silent exit classification
- [x] 2.2 Specify re-delegation with resume: reuse the `task_id`, prepend "Your previous attempt produced NO output. Retry N/3:" + full original delegation, and ALWAYS keep DELEGATION SUFFIX as the final block; reference `task_id` as correlation text in the message, do NOT pass it as a tool argument (rationale: determinism + framework-independence, NOT "subagent cannot access prior messages")
- [x] 2.3 Specify retry budget: max 3 retries (retryCount 1..3), backoff 2s/5s/10s (max 30s total) between attempts is best-effort SHOULD (apply when a pause is possible; the plugin/guardrail layer supplies the real retry value), escalate to the user after the 3rd exhausted attempt with agent/task/retryCount summary; retry envelopes SHALL use `responseType: "failure"`, `result: "retry"`, `retryCount: N`, AND include the `error` object (`code: "SILENT_EXIT"`, `message: "Subagent returned empty output, retrying delegation"`, `details: "<delegation summary>"`) per the orchestrator-retry-protocol spec scenario; the exhausted-retries escalation SHALL reuse `result: "escalated"` from `orchestrator.schema.json`
- [x] 2.4 Specify telemetry with a concrete bash mechanism: append the `subagent.silent_exit` event to `.opencode/logs/subagent-silent-exit-audit.jsonl` by executing `mkdir -p .opencode/logs && echo '<JSON-line-escaped>' >> .opencode/logs/subagent-silent-exit-audit.jsonl`; JSON payload fields: `eventType: "subagent.silent_exit"`, `timestamp` (ISO 8601), `session_id`, `delegatedAgent`, `retryCount` (integer 0..3), `failureReason` (`"empty_task_result"` | `"missing_envelope"`); JSON rules: double quotes only, no trailing comma, escape newlines/double quotes inside values, no markdown fences inside the payload; the orchestrator has bash in its tool policy; a write failure is non-fatal (note in escalation summary, continue the retry protocol)

## 3. Guardrail Rule

- [x] 3.1 Add rule `orchestrator-delegation-suffix-required` to `.opencode/guardrails-rules.ts` for tool `task`, following the existing 12-rule pattern (`{ name, description, tool, validate }`); validate that the prompt contains "DELEGATION SUFFIX (INJECTED BY ORCHESTRATOR)"; on missing marker return `{ allowed: false }` with message prefixed "GUARDRAIL_BLOCKED: " including a retry hint to append the suffix; first verify that the task tool args at `tool.execute.before` expose the prompt field (`args.prompt`) — if unextractable, fail open per the spec scenario (`{ allowed: true }` + `console.warn`)
- [x] 3.2 Register the rule in `TOOL_RULES["task"]` registry alongside existing task rules
- [x] 3.3 Add tests to `.opencode/guardrails-rules.test.js` covering: suffix present passes, suffix missing blocks with GUARDRAIL_BLOCKED prefix, retry hint present in violation message, and the unextractable-args case (missing/non-string prompt) fails open with `{ allowed: true }` and NO GUARDRAIL_BLOCKED error

## 4. Silent-Exit Telemetry Activation (plugin)

- [x] 4.1 Modify `.opencode/plugins/output-contracts.ts` null-extraction path: when `extractTaskResult` returns null (empty/unparseable `<task_result>`), append a JSONL entry to `.opencode/logs/contract-audit.jsonl` with `eventType: "silent_exit_candidate"`, `timestamp`, `agent`, `sessionId`, `task`, `retryCount: 0` — while still emitting the existing `console.warn`
- [x] 4.2 Ensure the audit-write failure path is non-fatal (existing pattern: catch + `console.error`, session continues)
- [x] 4.3 Add a "Subagent Silent Exit Audit" section to `docs/opencode/output-contracts.md` documenting the `subagent.silent_exit` orchestrator event (subagent-silent-exit-audit.jsonl) and the `silent_exit_candidate` plugin entry (contract-audit.jsonl), including field lists and the relationship (plugin marks candidates, orchestrator recovers); explicitly document the classification boundary: envelope-less responses (text, no `<output-contract>` envelope) produce a "contract-validation" entry (NOT "silent_exit_candidate"), and the orchestrator treats them as silent exit per the orchestrator-retry-protocol spec
- [x] 4.4 Unit test for silent_exit_candidate logging path
  - Test 1: mock `fs.appendFileSync` success → assert JSONL entry written with correct fields (timestamp, agent, sessionId, task, retryCount=0, eventType="silent_exit_candidate")
  - Test 2: mock `fs.appendFileSync` failure → assert no throw, console.error logged, session continues (non-fatal)
  - Test 3: valid envelope output → assert NO silent_exit_candidate entry written

## 5. Verification

- [x] 5.1 Run guardrails test suite: `npx vitest run` in `.opencode/` — confirm `orchestrator-delegation-suffix-required` tests pass and no regressions in the existing 12 rules (`.opencode/` has no npm test script and is not an npm workspace) — **33 tests pass**
- [x] 5.2 Run contract validator tests: `npm test` in `docs/opencode/prompts/contracts/` — confirm no regressions (includes the new prompt-hygiene test from 5.5) — **60 tests pass**
- [x] 5.3a Deterministic plugin-level test: invoke the hook's `tool.execute.after` with a crafted empty output (mocked `<task_result></task_result>` and missing `<task_result>` wrapper cases); assert `silent_exit_candidate` entry written + `console.warn` emitted + non-fatal write failure path tested — **Covered by 4.4 tests (Test 1, 2, 3, 5)**
- [~] 5.3b Optional live delegation check: exercise one re-delegation cycle with a real subagent configured to emit no output; verify (a) orchestrator detects empty `<task_result>`, (b) re-delegates with resume "Retry N/3", (c) `subagent.silent_exit` entry appears in `subagent-silent-exit-audit.jsonl`, (d) `silent_exit_candidate` entry appears in `contract-audit.jsonl` — **Skipped: no viable subagent available in this environment to produce empty output for live testing**
- [x] 5.4 Verify prompt guards: grep all 8 prompts in `docs/opencode/prompts/` for "Empty responses are NOT acceptable" inside a top-of-file `## CRITICAL RULES`; ALSO verify the invariant: `grep -n '^# CRITICAL RULES' docs/opencode/prompts/*.md` returns 0 hits — **All invariants verified: 0 legacy h1 hits, 8 files with anti-empty bullet, 3 Behavioral Rules, CRITICAL RULES at line 5 in all 8 files**
- [x] 5.5 Add vitest hygiene test `docs/opencode/prompts/contracts/prompt-hygiene.test.js` — **Created and passing (25 tests: 8 files × 3 checks + 1 meta test)**
- [x] 5.6 Verify guardrail blocks suffix-less delegation — **Covered by 3.3 tests: "suffix missing blocks with GUARDRAIL_BLOCKED prefix" + "unextractable-args case fails open"**