## 1. Workflow `security-digest.yml` — scaffolding y triggers

- [x] 1.1 Crear `.github/workflows/security-digest.yml` con trigger `schedule: cron '0 3 * * 1'` (lunes 03:00 UTC, mismo patrón que el sibling `ci-secret-scanning` — REUTILIZAR el trigger, NO duplicar `scheduled-security.yml`), `workflow_dispatch` para ejecución manual, y un input OPCIONAL `pull_request_number` (string, sin valor por defecto — respeta el spec "no required inputs"; habilita el PR summary comment de la task 4.5 vía dispatch manual). **Spec**: ci-scheduled-security-review → "Weekly scheduled security review trigger" + "Actionable digest posted as PR comment"
- [x] 1.2 Declarar `permissions` top-level: `contents: read` + `actions: read` (necesario para descargar el artifact `gitleaks-report` del run del sibling vía download-artifact/gh api). **Spec**: "Weekly scheduled security review trigger" + "Human-readable security digest generation"
- [x] 1.3 Declarar jobs: `sbom`, `vulnerability-review`, `digest` con `needs: [sbom, vulnerability-review]` en el job `digest` y condición `if: always() && !cancelled()` (semántica: corre salvo cancelación; si ambos upstream fallan el digest falla visiblemente y los artifacts de jobs completados quedan disponibles — design R5 y spec "Digest job failure handling"). **Spec**: todas las requirements

## 2. Job `sbom` — SBOM semanal del estado mergeado

- [x] 2.1 Job `sbom`: `actions/checkout@v5.0.0` (tag verificado) + `anchore/sbom-action@v0.17.2` con `format: cyclonedx-json` y `output-file: sbom-project-one.json` (misma action/formato que el sibling `ci-security-enhance`). **Spec**: "Scheduled SBOM generation on merged state"
- [x] 2.2 Subir el SBOM como artifact con `actions/upload-artifact@v4.6.2` (name: `sbom`, path: `sbom-project-one.json`, `if-no-files-found: error`, `retention-days: 365`). **Spec**: "Scheduled SBOM generation on merged state" → "SBOM uploaded for traceability"

## 3. Job `vulnerability-review` — osv-scanner sobre el lockfile

- [x] 3.1 Job `vulnerability-review`: `actions/checkout@v5.0.0` + `google/osv-scanner-action@v2.3.8` (**tag candidato — verificar contra el repo remoto en implementación; la review del planner no pudo confirmarlo**) escaneando `package-lock.json` con salida JSON (p.ej. `--format json` / `--json` según la action) a `osv-report.json`. **Spec**: "Scheduled dependency vulnerability and license review" → "Vulnerable package in merged dependencies"
- [x] 3.2 Poner el step del scan en modo auditoría (`continue-on-error: true`): el run no falla por hallazgos (consistente con el cron sibling de secret scanning). **Spec**: "Digest and reports surfaced without breaking automation"
- [x] 3.3 Subir `osv-report.json` como artifact con `actions/upload-artifact@v4.6.2` (name: `osv-report`) para inspección manual. **Spec**: "Scheduled dependency vulnerability and license review"

## 4. Job `digest` — cross-ref de secretos + generación del digest

- [x] 4.1 Resolver el run más reciente del workflow `scheduled-security.yml` del sibling (vía `gh api repos/{owner}/{repo}/actions/workflows/scheduled-security.yml/runs?per_page=1` o `gh run list --workflow scheduled-security.yml -L 1`) y descargar el artifact `gitleaks-report` con `actions/download-artifact@v4.2.1` (o `gh run download`) — degradación elegante si no existe. **Spec**: "Human-readable security digest generation" → "Secret-scanning findings cross-referenced"
- [x] 4.1b Descargar los artifacts del MISMO run con `actions/download-artifact@v4.2.1` (default: run actual): `sbom` (task 2.2) y `osv-report` (task 3.3) — el job `digest` los necesita como input del script; un download olvidado falla silenciosamente el digest (mitigado solo por R5). **Spec**: "Human-readable security digest generation" → "Digest generated from scheduled run"
- [x] 4.2 Crear `scripts/security/generate-security-digest.mjs` (Node >= 20, pure JS, `node:fs`/`node:path` built-ins, SIN deps npm) que parsee `sbom-project-one.json` + `osv-report.json` + (opcional) `gitleaks-report.json` y emita `security-digest.md` con: total de dependencias, paquetes vulnerables con severidad, resumen de licencias (derivado del SBOM), timestamp, y estado del cross-ref de secretos ("secret report unavailable" si falta). **Política de licencias**: constante estática `LICENSE_DENY_LIST` en el script con **15 entradas** (valores reales copiados del deny-list de la action, no lista ilustrativa) cubriendo la familia GPL/LGPL/AGPL: versiones base + variantes `-+` "or later" de GPL/AGPL, y versiones base de LGPL (LGPL-1.0, LGPL-2.0, LGPL-2.1, LGPL-3.0). **DESVIACIÓN ACEPTADA vs el default de `actions/dependency-review-action` (18 entradas)**: se omiten intencionalmente `LGPL-2.0+`, `LGPL-2.1+`, `LGPL-3.0+` — la familia LGPL base ya está cubierta y las variantes `-+` se omiten para evitar sobre-bloqueo. Documentar la deny-list en `docs/security/SECURITY.md` (task 5.1). **Spec**: "Scheduled dependency vulnerability and license review" → "License summary derived"
- [x] 4.3 Ejecutar el script en el job con `node scripts/security/generate-security-digest.mjs` (runner node o `actions/setup-node@v4.4.0` — tag verificado — con `.nvmrc`). **Spec**: "Human-readable security digest generation"
- [x] 4.4 Subir `security-digest.md` como artifact con `actions/upload-artifact@v4.6.2` (name: `security-digest`). **Spec**: "Human-readable security digest generation" → "Digest generated from scheduled run"
- [x] 4.5 (Opcional) Step condicional `if: inputs.pull_request_number != ''` (o `github.event_name == 'pull_request'` para extensión futura — el trigger set actual NO incluye `pull_request`, así que `event_name` solo nunca se cumple) que postee el resumen del digest como PR summary comment en el PR del input `pull_request_number` (requiere `pull-requests: write` JOB-SCOPED en el job `digest`; se omite en cron — no hay PR). El step DEBE incluir gate de accionabilidad (solo postear si hay hallazgos critical/high o licencias deny-list — evita ruido en digests limpios; spec R4 WHEN) y el comentario DEBE incluir referencia al artifact `security-digest` para detalles completos (spec R4 THEN). **Spec**: "Human-readable security digest generation" → "Actionable digest posted as PR comment"

## 5. Documentación

- [x] 5.1 Añadir sección "Scheduled security digest" en `docs/security/SECURITY.md`: cadencia semanal (lunes 03:00 UTC), qué corre (SBOM + osv-scanner + digest), cómo leer el digest, dónde están los artifacts, y **documentar la deny-list de licencias `LICENSE_DENY_LIST` del script** (task 4.2 — spec R3 exige política documentada). **Spec**: todas

## 6. Verificación

- [~] 6.1 Validar sintaxis del workflow con `docker run rhysd/actionlint:latest` (o parser YAML como fallback) en `.github/workflows/security-digest.yml` — **DEFERRED a CI run per orchestrator 2026-08-02 (docker no disponible localmente)**
- [~] 6.2 Ejecutar manualmente el workflow (`workflow_dispatch`) y confirmar: artifact `sbom`, artifact `osv-report`, artifact `security-digest` con contenido markdown correcto — **DEFERRED a workflow_dispatch manual post-merge per orchestrator 2026-08-02**
- [~] 6.3 Confirmar el cross-ref de secretos: si `ci-secret-scanning` ya está mergeado y tiene un run semanal con artifact `gitleaks-report`, el digest lo incluye; si no, el digest dice "secret report unavailable" SIN fallar el run — **DEFERRED a primer run semanal post-merge per orchestrator 2026-08-02**
- [~] 6.4 Verificar que NO se crearon/modificaron `.github/workflows/scheduled-security.yml` ni `.github/workflows/security.yml` (archivos de siblings) — **DEFERRED: verifiable via git diff at commit time per orchestrator 2026-08-02**
- [~] 6.5 Ejecutar `openspec validate "ci-scheduled-security" --strict` y confirmar que pasa antes de archivar — **DEFERRED to pre-archive verification per orchestrator 2026-08-02**

## 7. Merge order y dependencias cross-change

- [x] 7.1 **Merge-order note**: este change DEBE mergearse DESPUÉS de `ci-secret-scanning` (provee `scheduled-security.yml` + artifact `gitleaks-report` para el cross-ref — dependencia RUNTIME dura) y de `ci-security-enhance` (provee job `sbom`/`dependency-review` en `security.yml` — dependencia SOLO de orden/consistencia: este change NO consume artifacts de `security.yml` porque re-ejecuta su propio SBOM). Verificar que ambos están en `main` antes de abrir el PR de este change
- [x] 7.2 **Dependabot note**: `ci-secret-scanning` task 4.4 crea `.github/dependabot.yml` (ecosistema `github-actions` [+ docker]); `ci-test-integration` task 13.1 lo crea con ecosistema `npm`. Al mergear este change, NO crear otro `.github/dependabot.yml` — verificar que los pines nuevos (`anchore/sbom-action@v0.17.2`, `google/osv-scanner-action@v2.3.8`, `actions/upload-artifact@v4.6.2`, `actions/download-artifact@v4.2.1`) quedan cubiertos por el ecosistema `github-actions` existente
- [x] 7.3 Confirmar que el script `scripts/security/generate-security-digest.mjs` no introduce dependencias npm nuevas (solo built-ins de Node) y que no se modifica `package.json`
